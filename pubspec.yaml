name: flutter_for_web_push_notification_sample
description: "Flutter for Web Push Notification Sample"
publish_to: 'none'

version: 1.0.0+1
environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  cupertino_icons: ^1.0.6
  flutter:
    sdk: flutter
  flutter_hooks: ^0.20.3
  js: ^0.6.7

dev_dependencies:
  pedantic_mono: ^1.25.0

flutter:
  uses-material-design: true
